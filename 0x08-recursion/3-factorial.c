#include "main.h"

/**
 * factorial - return the factorial of a given number
 * @n: value of integer
 * Return: int
 */

int factorial(int n)
{
	if (n == 1)
	{
		return (1);
	}
	else if
	{
		return (n * factorial(n - 1));
	}
	else if (n < 0)
	{
		return (-1);

}

