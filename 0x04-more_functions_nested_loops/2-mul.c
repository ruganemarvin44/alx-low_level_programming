#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: first integer
 * @b: second integer
 * prod is the product of the integers
 * Return: 0 (Always)
 *
 */
int mul(int a, int b)
{
return (a * b);
}
