#include <stdio.h>
#include <stdlib.h>
/**
 * main - main function prints gifits of base  ten
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d \n", i);
	return (0);
}
