#include <stdio.h>
#include <stdlib.h>
/**
 * main - main function prints digits of base ten
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
