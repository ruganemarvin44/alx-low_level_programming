#include <stdio.h>

/**
 * main - main function prints digits of base ten
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
