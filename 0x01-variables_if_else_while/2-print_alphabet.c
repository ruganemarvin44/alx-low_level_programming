#include <stdio.h>
#include <stdlib.h>
#include <stdout.h>
/**
 * main - main function prints alphabetic in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
	char i = {a..z}
	{
		putchar(i);
	}

	return (0);
}
